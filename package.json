{
  "name": "wcs_cda_checkpoint_country_app",
  "version": "1.0.0",
  "description": "checkpoint",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format:fix": "prettier --write \"**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
